# telephony_context example

Demonstrates `TelephonyContextPlugin.getContext()` on Android. Tap **Load telephony context** to print JSON (including a Dart-side `collectedAt` timestamp).

Run:

```bash
flutter run
```
